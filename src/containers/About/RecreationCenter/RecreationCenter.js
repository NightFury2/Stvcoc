import React, { Component } from 'react';

import {Card, CardHeader, CardText} from 'material-ui/Card';
import Divider from 'material-ui/Divider';

export default class RecreationCenter extends Component {
  render() {
    return (
       <Card>
          <CardHeader
             title="База отдыха"
          />
          <Divider />
          <CardText>
            sdfs
          </CardText>
        </Card>
    );
  }
}
